import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  statusContainer: {
    flexDirection: "row",
    justifyContent: "space-around",
  },
  chip: {
    marginVertical: 15,
  },
});
