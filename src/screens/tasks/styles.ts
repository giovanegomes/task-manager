import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    padding: 10,
  },
  fab: {
    margin: 15,
  },
  submitButton: {
    marginRight: 10,
  },
});
